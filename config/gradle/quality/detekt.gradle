def comma = ","

detekt {

    toolVersion = Versions.PLUGIN_DETEKT
    config = files("${project.rootDir}/config/quality/detekt.yml")
    filters = ".*/test/.*" + comma +
            ".*/robolectricTest/.*" + comma +
            ".*/androidTest/.*" + comma +
            ".*/resources/.*"
    disableDefaultRuleSets = false
    parallel = true

}
